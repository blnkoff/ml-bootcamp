# ------------------------------------------------------------
# API Server — main ClearML REST API
# ------------------------------------------------------------
# Responsible for communication between clients (agents, SDK)
# and the server (experiments, tasks, models, artifacts, etc.)
# Listens on port 8052 by default.
CLEARML_APISERVER_PORT=8052

# ------------------------------------------------------------
# File Server — storage for artifacts and logs
# ------------------------------------------------------------
# Used for uploading/downloading experiment files,
# models, artifacts, and datasets.
# Listens on port 8050 by default.
CLEARML_FILESERVER_PORT=8050

# ------------------------------------------------------------
# Web Server — ClearML Web UI
# ------------------------------------------------------------
# Interface for viewing experiments, projects, agents,
# models, and more.
# Listens on port 8051 by default.
CLEARML_WEBSERVER_PORT=8051

# ------------------------------------------------------------
# Host machine (usually localhost or server IP)
# ------------------------------------------------------------
# If the server is deployed locally, leave as 127.0.0.1.
# If the server is externally accessible, specify its
# public IP address or domain name.
CLEARML_HOST_IP=127.0.0.1

# ------------------------------------------------------------
# Combined service endpoints
# ------------------------------------------------------------
# These variables are typically used by ClearML SDK
# and agents to connect to the respective server components.
# ------------------------------------------------------------

# REST API endpoint
CLEARML_API_HOST=http://${CLEARML_HOST_IP}:${CLEARML_APISERVER_PORT}

# Web UI endpoint (ClearML dashboard)
CLEARML_WEB_HOST=http://${CLEARML_HOST_IP}:${CLEARML_WEBSERVER_PORT}

# File storage endpoint (for artifacts)
CLEARML_FILES_HOST=http://${CLEARML_HOST_IP}:${CLEARML_FILESERVER_PORT}

# Credentials
CLEARML_USERNAME=clearml_user
CLEARML_PASSWORD=clearml_password
